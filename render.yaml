services:
  - type: web
    name: areteseek-backend
    runtime: node
    region: singapore
    plan: free
    buildCommand: rm -f package-lock.json && npm install --force && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 1337
